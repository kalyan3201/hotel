apiVersion: v1
kind: Service
metadata:
  name: hotel-service
spec:
  type: NodePort
  selector:
    app: hotel
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
